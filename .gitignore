node_modules/
dist/
build/
.vite/
*.local
.DS_Store
Thumbs.db
desktop.ini
*.pem

# === Next.js (webapp spostata in radice) ===
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# === OpenNext / Cloudflare Workers ===
.open-next
.wrangler
.vercel

# === Variabili d'ambiente: mai valori reali, gli .example restano tracciati ===
.env*
!.env.example
.dev.vars*
!.dev.vars.example

# === Prisma client generato (rigenerabile con `prisma generate`) ===
/src/generated/prisma

# === package manager alternativi, non usati qui ma innocui da escludere ===
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

/coverage

# === Claude standard privato ===
_notes/
CLAUDE.local.md
.claude/settings.local.json
.claude/memory/*.local.md
